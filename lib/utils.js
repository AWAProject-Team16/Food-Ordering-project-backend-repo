const utils = {

  // isAccountTypeValid: account_type => {
  //   account_type = String(account_type).trim().toLowerCase();
  //   return account_type === 'manager' || account_type === 'consumer';
  // },

  // convertAccountTypeToNumber: account_type => {
  //   account_type = String(account_type).trim().toLowerCase();
  //   if (utils.isAccountTypeValid(account_type)) return account_type === 'manager' ? 0 : 1;
  // },

  // convertAccountTypeToString: account_type_number => {
  //   if (!Number.isNaN(account_type_number)) return account_type_number === 0 ? 'manager' : 'consumer';
  // }
  
}


module.exports = utils;